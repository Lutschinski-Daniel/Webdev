{% extends 'base.html.twig' %}

{% block body %}
    <div class="form-padd">
        <div class="row">
            <div class="col-sm-3"></div>
            <div class="col-sm-6">
                <h2>Register</h2>
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
           </div>
           <div class="col-sm-3"></div>
        </div>
    </div>
{% endblock %}